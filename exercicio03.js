/**
 * Crie duas variáveis: preco e quantidade.
 * Exiba o valor total da compra no console.
 * Armazene o valor total em uma variável antes 
 * de mostrar para o usuário! 
 * valorTotal = preco * quantidade;
 */

let preco = 10;
let quantidade = 5;

let valorTotal = preco * quantidade; // CamelCase

console.log("Valor total:", valorTotal);